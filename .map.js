/**map:The .map() method returns a new array containing 
 * the results of applying the provided function to each element in the original array.
 */
const numbers = [1, 2, 3];
const doubled = numbers.map(num => num * 2);
console.log(doubled); 
/**output:[2, 4, 6]*/

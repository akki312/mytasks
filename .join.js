/**.join:it joins all the elements in 
 * the given array or string and dispplays
 *  before and after joining */
const elements = ['Fire', 'Air', 'Water'];
console.log(elements.join()); 
console.log(elements.join('')); 
/**output:fir,air,water
       firairwater*/
